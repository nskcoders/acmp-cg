#include <iostream>
#define x std::cin  >>
#define z while(j >= i++)

int i, j, a[1002];
main(){
    x j; z x a[i];

    while(x i >> j) z std::cout << a[i - 1] << (j + 1 - i ? ' ' : '\n');
}
