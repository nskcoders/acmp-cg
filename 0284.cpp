#include <iostream>
#define x std::cin  >>
#define y std::cout <<

int n, i, j, a[1000];
main() {
    x n;
    while(n - i++) x a[i - 1];

    x n;
    while(x i >> j){
        while(j + 1 - i++) y a[i - 2] << ' ';
        y '\n';
    }
}
